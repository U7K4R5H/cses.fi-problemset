#include <bits/stdc++.h>
using namespace std;

#define ll long long

//push https://github.com/U7K4R5H/cses.fi-problemset.git master

bool isEven(int n) { return (n % 2 == 0); }

int main() {

	int t = 0;
	cin >> t;
	while (t--) {

		ll x, y;
		cin >> x >> y;

		ll mx = 0;
		ll ans = 0;

		mx = max(x, y);


		cout << ans << endl;
	}
}